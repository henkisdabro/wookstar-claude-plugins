{
  "mcpServers": {
    "mikrotik": {
      "command": "uvx",
      "args": [
        "mcp-server-mikrotik",
        "--host",
        "${MIKROTIK_HOST}",
        "--username",
        "${MIKROTIK_USER}",
        "--password",
        "${MIKROTIK_PASSWORD}",
        "--port",
        "2200"
      ]
    }
  }
}
